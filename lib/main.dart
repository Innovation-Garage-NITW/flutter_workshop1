import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      title: "Flutter Workshop",
      home: Scaffold(appBar: AppBar(), body: Text('Flutter Workshop')),
    ),
  );
}
